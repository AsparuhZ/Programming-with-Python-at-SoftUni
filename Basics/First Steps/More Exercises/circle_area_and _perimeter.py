from math import pi

r = float(input())
radius = r

calculated_area = pi * radius * radius
calculated_parameter = 2 * pi * radius

print(f"{calculated_area:.2f}")
print(f"{calculated_parameter:.2f}")
